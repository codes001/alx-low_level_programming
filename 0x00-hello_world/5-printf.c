#include <stdio.h>
/**
 * main - main is where the code execution start, and it prints the output
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
